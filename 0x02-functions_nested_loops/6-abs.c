#include "main.h"

/**
 * _abs - computes absolute vale of an integer
 * @n: the integer passed
 * Return: return absolute valeu (int)
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
