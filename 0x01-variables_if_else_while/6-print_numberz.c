#include <stdio.h>

/**
 * main - single digit
 * Description: print all single digit nums
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
